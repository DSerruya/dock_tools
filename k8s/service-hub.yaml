apiVersion: v1
kind: Service
metadata:
  name: utility-tools-hub
  namespace: dock-tools
spec:
  selector:
    app: utility-tools-hub
  type: ClusterIP
  ports:
    - name: http
      port: 3000
      targetPort: 3000
